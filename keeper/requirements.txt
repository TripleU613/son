# Pinned to what this was tested against. playwright's version must match the
# base image's bundled browsers -- the image is v1.62.0, so this is too, or
# Playwright refuses to launch with a version-mismatch error.
playwright==1.62.0
httpx==0.28.1
# R2, over the S3 API. boto3 rather than the aws CLI: `awscli` has no
# installation candidate on this image's Ubuntu base.
boto3==1.42.11
